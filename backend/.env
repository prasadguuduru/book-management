# Backend Environment Variables for Local Development (LocalStack)

# Environment
NODE_ENV=local
BACKEND_PORT=3001

# AWS Configuration (LocalStack)
AWS_REGION=us-east-1
AWS_ENDPOINT_URL=http://localhost:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# DynamoDB Configuration
TABLE_NAME=local-ebook-platform
DYNAMODB_ENDPOINT=http://localhost:4566

# JWT Configuration (Development keys - DO NOT use in production)
JWT_PUBLIC_KEY=dummy-public-key-for-local-development
JWT_PRIVATE_KEY=dummy-private-key-for-local-development
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Encryption (Development key - DO NOT use in production)
ENCRYPTION_KEY=dummy-encryption-key-32-characters

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,https://m6ldi0art9.execute-api.localhost.localstack.cloud:4566

# Logging
LOG_LEVEL=debug

# S3 Configuration
ASSETS_BUCKET=local-ebook-assets
FRONTEND_BUCKET=local-ebook-frontend

# SNS/SQS Configuration
NOTIFICATION_TOPIC_ARN=arn:aws:sns:us-east-1:000000000000:local-user-notifications
WORKFLOW_QUEUE_URL=http://localhost:4566/000000000000/local-book-workflow-queue

# Email Configuration (SES)
SES_REGION=us-east-1
FROM_EMAIL=noreply@localhost