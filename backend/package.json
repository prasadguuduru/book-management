{
  "name": "book-management-backend",
  "version": "1.0.0",
  "description": "Book Management System Backend",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "package": "npm run build && cd dist/functions/hello && zip -r ../../../hello.zip .",
    "deploy:local": "npm run package && ../scripts/deploy-local.sh"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@types/aws-lambda": "^8.10.92",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/uuid": "^9.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
